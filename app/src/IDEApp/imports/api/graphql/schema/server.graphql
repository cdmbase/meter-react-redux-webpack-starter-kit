scalar Url
scalar Date

type Server {
  _id: ID,
  name: String,
  url: Url,
  status: String
  info: Info
  createdAt: Date
  updatedAt: Date
}

type Info {
  port: String,
}

extend type Query {
  server(id: String): Server
}
